---
phase: 01-foundation-thread-safety-robustness-test-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Tests/Editor/DMS.Tests.Editor.asmdef
  - Tests/Runtime/DMS.Tests.Runtime.asmdef
  - Tests/Editor/DSMTestConfig.cs
  - Tests/Editor/DSMTestRunnerSmokeTests.cs
  - Tests/Editor/DSMSlotConcurrencyTests.cs
  - Runtime/DSMSlot.cs
  - Runtime/DSMSlotManager.cs
autonomous: true
requirements: [TEST-01, CONC-01, CONC-04, TEST-03]
user_setup: []

must_haves:
  truths:
    - "Unity EditMode Test Runner discovers and runs DSM test assemblies (previously zero tests existed)"
    - "100 concurrent Set() calls with distinct keys leave every key present with its correct value — asserted as final-state correctness, not just absence of exceptions"
    - "A Set() -> Save() -> new-slot Load() disk roundtrip returns the written value, proving the real persistence path end-to-end"
    - "DSMSlot._data and DSMSlotManager._slots mutations are guarded so concurrent access cannot corrupt or drop entries"
  artifacts:
    - "Tests/Editor/DMS.Tests.Editor.asmdef"
    - "Tests/Runtime/DMS.Tests.Runtime.asmdef"
    - "Tests/Editor/DSMTestConfig.cs (reflection-based DSMConfig fixture builder)"
    - "Tests/Editor/DSMSlotConcurrencyTests.cs"
    - "private readonly object _dataLock in DSMSlot"
    - "private readonly object _slotsLock in DSMSlotManager"
  key_links:
    - "Test asmdefs reference DMS.Runtime (GUID 5c67e4f53d76c46cbabd0a518ee024c0) + UniTask (GUID f51ebe6a0ceec4240a699833d6309b23) so tests can await UniTask APIs and call DSMSlot"
    - "DSMTestConfig sets private DSMConfig fields (_autoSave=false) via reflection so the concurrency test isolates the _data lock from the debounce path (fixed in Plan 02)"
    - "_watcher.Notify() and ScheduleSave() are called OUTSIDE lock(_dataLock) (snapshot-then-release shape from DSMWatcher.Notify)"
---

<objective>
Slice 1 (Walking Skeleton). Stand up the package's first EditMode test infrastructure, prove it runs, then land the root synchronization fix on shared slot state (D-04, D-05 sync half) verified by a green final-state-correctness regression test.

Purpose: Every later capability in this milestone (encryption rotation, schema validation, migration, editor tooling) reads and writes through `DSMSlot._data` and `DSMSlotManager._slots`. Synchronizing them first — and proving the test harness works — is the foundation the rest of the phase and milestone build on.
Output: `Tests/Editor` + `Tests/Runtime` asmdefs, a config fixture builder, a smoke test, a concurrency regression test, and the `_data`/`_slots` locks that make it pass.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-thread-safety-robustness-test-infrastructure/01-CONTEXT.md
@.planning/phases/01-foundation-thread-safety-robustness-test-infrastructure/01-PATTERNS.md
@.planning/phases/01-foundation-thread-safety-robustness-test-infrastructure/01-SKELETON.md
@.planning/codebase/TESTING.md
@Runtime/DSMSlot.cs
@Runtime/DSMSlotManager.cs
@Runtime/DSMWatcher.cs
@Runtime/DSMConfig.cs
</context>

## Phase Goal

**As a** game developer using DSM, **I want to** keep the save system's shared state and save/load pipeline safe under concurrent access and invalid input, **so that** my players never lose or corrupt their save data.

(Faithful user-story reframing of the ROADMAP outcome goal for this phase — see the note in the planner return; no new scope introduced.)

## Acceptance Criteria (Slice 1)
- [ ] Unity EditMode Test Runner discovers both new test assemblies and runs the smoke test green.
- [ ] `ConcurrentSet_WithDistinctKeys_AllKeysPresentWithCorrectValues` passes (final-state correctness).
- [ ] `SetSaveLoad_Roundtrip_PersistsValue` passes (real disk write + read).
- [ ] `DSMSlot._data` and `DSMSlotManager._slots` access is lock-guarded per D-04/D-05.

## Artifacts this phase produces (Plan 01 additions)
- `Tests/Editor/DMS.Tests.Editor.asmdef`, `Tests/Runtime/DMS.Tests.Runtime.asmdef` (new test assemblies, EditMode-only)
- `Tests/Editor/DSMTestConfig.cs` — static fixture builder `DSMTestConfig.Create(...)`
- `Tests/Editor/DSMTestRunnerSmokeTests.cs` — smoke fixture
- `Tests/Editor/DSMSlotConcurrencyTests.cs` — `[TestFixture] DSMSlotConcurrencyTests`
- `DSMSlot._dataLock` field; `DSMSlotManager._slotsLock` field

<tasks>

<task type="auto">
  <name>Task 1: Create EditMode test assemblies + config fixture + smoke test (TEST-01, D-06)</name>
  <files>Tests/Editor/DMS.Tests.Editor.asmdef, Tests/Runtime/DMS.Tests.Runtime.asmdef, Tests/Editor/DSMTestConfig.cs, Tests/Editor/DSMTestRunnerSmokeTests.cs</files>
  <read_first>
    - `.planning/phases/01-foundation-thread-safety-robustness-test-infrastructure/01-PATTERNS.md` lines 280-368 (asmdef target shapes for both test assemblies) and lines 15-16 (asmdef analogs)
    - `.planning/codebase/TESTING.md` lines 48-108 (NUnit `[TestFixture]`/`[SetUp]`/`[TearDown]` structure) and lines 145-188 (fixture builder + temp-directory patterns)
    - `Runtime/DSMConfig.cs` lines 11-38 (private `[SerializeField]` fields `_autoSave`, `_autoSaveDebounce`, `_encrypt`, `_savePath` that the fixture sets by reflection)
  </read_first>
  <action>
    Create `Tests/Editor/DMS.Tests.Editor.asmdef` with: name "DMS.Tests.Editor"; references the DMS.Runtime assembly (GUID:5c67e4f53d76c46cbabd0a518ee024c0), UniTask (GUID:f51ebe6a0ceec4240a699833d6309b23), UniTask.Linq (GUID:5c01796d064528144a599661eaab93a6), Newtonsoft.Json (GUID:6055be8ebefd69e48b49212b09b47b2f), plus assembly-name references "UnityEngine.TestRunner" and "UnityEditor.TestRunner"; includePlatforms ["Editor"]; overrideReferences true; precompiledReferences ["nunit.framework.dll"]; autoReferenced false; defineConstraints ["UNITY_INCLUDE_TESTS"]. Create `Tests/Runtime/DMS.Tests.Runtime.asmdef` identically except name "DMS.Tests.Runtime" (per D-06 it still uses includePlatforms ["Editor"] — Tests/Runtime is an asmdef referencing the Runtime assembly, NOT PlayMode execution). Both GUID sets are the resolved values for this project (verified from the package cache), replacing the placeholder GUIDs in PATTERNS.md.
    Create `Tests/Editor/DSMTestConfig.cs`: a static class `DSMTestConfig` with `public static DSMConfig Create(bool autoSave = false, float autoSaveDebounce = 0.05f, bool encrypt = false, string? savePath = null)` that does `ScriptableObject.CreateInstance<DSMConfig>()` then sets the private instance fields `_autoSave`, `_autoSaveDebounce`, `_encrypt`, and `_savePath` via `typeof(DSMConfig).GetField(name, BindingFlags.NonPublic | BindingFlags.Instance).SetValue(...)`. Default `autoSave:false` so tests isolate in-memory behavior from the AutoSave debounce path (the debounce path is rewritten in Plan 02).
    Create `Tests/Editor/DSMTestRunnerSmokeTests.cs`: `[TestFixture] public class DSMTestRunnerSmokeTests` with one `[Test] public void TestRunner_IsWired_Passes()` calling `Assert.Pass()`, and one `[Test] public void DSMTestConfig_Create_AppliesAutoSaveFalse()` that builds a config via `DSMTestConfig.Create()` and asserts `config.AutoSave` is false — proving the runtime assembly is referenceable and the fixture works.
    Do NOT create `.meta` files (Unity generates them on import). Follow CONVENTIONS.md `#nullable enable` + `DSM` type-name-prefix conventions.
  </action>
  <verify>
    <automated>python3 -c "import json,sys; [json.load(open(p)) for p in ['Tests/Editor/DMS.Tests.Editor.asmdef','Tests/Runtime/DMS.Tests.Runtime.asmdef']]; d=json.load(open('Tests/Editor/DMS.Tests.Editor.asmdef')); assert d['includePlatforms']==['Editor'] and 'nunit.framework.dll' in d['precompiledReferences'] and d['defineConstraints']==['UNITY_INCLUDE_TESTS'] and any('5c67e4f53d76c46cbabd0a518ee024c0' in r for r in d['references']); print('asmdef OK')"</automated>
    <automated>/Applications/Unity/Hub/Editor/6000.4.7f1/Unity.app/Contents/MacOS/Unity -runTests -batchmode -projectPath /Users/draftsama/Works/Unity/counter-stack-game -testPlatform EditMode -testFilter "DSMTestRunnerSmokeTests" -testResults /tmp/dsm-p01-t1.xml -logFile /tmp/dsm-p01-t1.log; grep -q 'result="Passed"' /tmp/dsm-p01-t1.xml</automated>
  </verify>
  <acceptance_criteria>
    - Both asmdef files are valid JSON, EditMode-only, with nunit precompiled reference and the UNITY_INCLUDE_TESTS define constraint.
    - The Editor test asmdef references DMS.Runtime by GUID 5c67e4f53d76c46cbabd0a518ee024c0.
    - `DSMTestRunnerSmokeTests` is discovered and both its tests pass in EditMode.
    - `DSMTestConfig.Create()` returns a config whose `AutoSave` is false.
  </acceptance_criteria>
  <done>Two test assemblies compile, are discovered by the Unity Test Runner, and the smoke fixture + fixture-builder test run green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write failing concurrency regression test + real Save/Load roundtrip (CONC-04, TEST-03 — RED)</name>
  <files>Tests/Editor/DSMSlotConcurrencyTests.cs</files>
  <behavior>
    - ConcurrentSet_WithDistinctKeys_AllKeysPresentWithCorrectValues: run 100 `Task.Run(() => slot.Set($"key{i}", i))` via `Task.WhenAll`; after completion every `slot.Get($"key{i}", -1)` equals `i`. Against the current unsynchronized `_data` this can drop/overwrite entries or throw from concurrent Dictionary mutation (RED).
    - SetSaveLoad_Roundtrip_PersistsValue: `slot.Set("hp", 42)`, `slot.Save()`, construct a second `DSMSlot` with the same name + temp dir, `Load()`, assert `Get("hp", 0) == 42` — a real disk write + read, GREEN even pre-lock (proves the persistence path).
  </behavior>
  <read_first>
    - `.planning/phases/01-foundation-thread-safety-robustness-test-infrastructure/01-PATTERNS.md` lines 372-424 (concurrency test template + adapted `DSMSlot` constructor signature) and lines 491-493 (NUnit structure source)
    - `Runtime/DSMSlot.cs` lines 23-30 (constructor `DSMSlot(string slotName, DSMConfig config, DSMSerializer serializer, string saveDirectory, Type? constantType)`), lines 32-62 (Set/Save)
    - `.planning/codebase/TESTING.md` lines 173-188 (temp-directory `[SetUp]`/`[TearDown]`)
  </read_first>
  <action>
    Create `Tests/Editor/DSMSlotConcurrencyTests.cs` as `[TestFixture] public class DSMSlotConcurrencyTests`. In `[SetUp]` build `_config = DSMTestConfig.Create()` (AutoSave false), `_serializer = new DSMSerializer()`, and a unique `_tempDir` under `Path.GetTempPath()`; in `[TearDown]` delete `_tempDir` recursively. Add `[Test] public async Task ConcurrentSet_WithDistinctKeys_AllKeysPresentWithCorrectValues()` using `System.Linq.Enumerable.Range(0, 100)` mapped to `Task.Run(() => slot.Set($"key{i}", i))`, awaited with `Task.WhenAll`, then assert each `slot.Get($"key{i}", -1)` equals `i` per PATTERNS.md lines 408-421. Add `[Test] public void SetSaveLoad_Roundtrip_PersistsValue()` per the behavior block, constructing two `DSMSlot` instances over the same name and `_tempDir`. Use `async Task` NUnit methods; do not use blocking `.GetAwaiter().GetResult()`. This task is RED for the concurrency test against current `Runtime/DSMSlot.cs`; do not modify Runtime code here.
  </action>
  <verify>
    <automated>/Applications/Unity/Hub/Editor/6000.4.7f1/Unity.app/Contents/MacOS/Unity -runTests -batchmode -projectPath /Users/draftsama/Works/Unity/counter-stack-game -testPlatform EditMode -testFilter "DSMSlotConcurrencyTests.SetSaveLoad_Roundtrip_PersistsValue" -testResults /tmp/dsm-p01-t2.xml -logFile /tmp/dsm-p01-t2.log; grep -q 'result="Passed"' /tmp/dsm-p01-t2.xml</automated>
  </verify>
  <acceptance_criteria>
    - `DSMSlotConcurrencyTests` compiles and is discovered.
    - `SetSaveLoad_Roundtrip_PersistsValue` passes (real disk roundtrip).
    - `ConcurrentSet_WithDistinctKeys_AllKeysPresentWithCorrectValues` exists and asserts per-key final-state correctness (it may fail/flake here — that is the intended RED baseline, made GREEN in Task 3).
  </acceptance_criteria>
  <done>The concurrency fixture exists; the roundtrip test is green; the concurrency test documents the RED baseline (final-state assertion, not "did not throw").</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Synchronize DSMSlot._data and DSMSlotManager._slots (CONC-01, D-04, D-05 sync half — GREEN)</name>
  <files>Runtime/DSMSlot.cs, Runtime/DSMSlotManager.cs</files>
  <behavior>
    - After the fix, `ConcurrentSet_WithDistinctKeys_AllKeysPresentWithCorrectValues` passes deterministically.
    - `Set`/`Get`/`Has`/`Delete`/`Clear` and `SeedDefaults` never expose a partially-mutated `_data`.
    - `GetOrCreateSlot`/`DeleteSlot` never race the `_slots` dictionary (no lost creations, no double instances).
  </behavior>
  <read_first>
    - `.planning/phases/01-foundation-thread-safety-robustness-test-infrastructure/01-PATTERNS.md` lines 22-81 (DSMSlot lock target shapes), lines 165-208 (DSMSlotManager coarse lock), lines 452-463 (shared lock pattern)
    - `Runtime/DSMWatcher.cs` lines 10-11, 42-73 (the codebase's only lock precedent: snapshot-under-lock then iterate outside)
    - `Runtime/DSMSlot.cs` lines 32-50 (Set/Get/Has/Delete/Clear), lines 123-134 (SeedDefaults)
    - `Runtime/DSMSlotManager.cs` lines 35-43 (DeleteSlot), lines 65-71 (GetOrCreateSlot)
  </read_first>
  <action>
    In `Runtime/DSMSlot.cs`: add `private readonly object _dataLock = new();`. Wrap each `_data` access in `Set`/`Get`/`Has`/`Delete`/`Clear` (lines 32-50) in `lock (_dataLock) { ... }` covering only the dictionary read/write; keep `_watcher.Notify(key, value)` and `ScheduleSave()` OUTSIDE the lock, called after release (mirrors DSMWatcher.Notify snapshot-then-release at lines 42-52). For `Get`, read the token inside the lock into a local, then call `ToObject<T>` outside the lock. In `SeedDefaults()` (lines 123-134) guard the `_data = new Dictionary...` reassignment and the per-field `_data[field.Name] = ...` writes under `lock (_dataLock)`. Do not hold `_dataLock` across any `await` or file I/O.
    In `Runtime/DSMSlotManager.cs`: add `private readonly object _slotsLock = new();`. Wrap the `_slots.TryGetValue`/`_slots[name] = slot` body of `GetOrCreateSlot` (lines 65-71) in `lock (_slotsLock)`, and wrap only the `_slots.Remove(name)` line of `DeleteSlot` (line 37) in `lock (_slotsLock)` — keep the file-delete I/O (lines 38-42) OUTSIDE the lock (D-04, PATTERNS.md line 208).
    Use plain `lock` only (D-05 sync half); do NOT introduce `SemaphoreSlim` here (that is Plan 02).
  </action>
  <verify>
    <automated>/Applications/Unity/Hub/Editor/6000.4.7f1/Unity.app/Contents/MacOS/Unity -runTests -batchmode -projectPath /Users/draftsama/Works/Unity/counter-stack-game -testPlatform EditMode -testFilter "DSMSlotConcurrencyTests" -testResults /tmp/dsm-p01-t3.xml -logFile /tmp/dsm-p01-t3.log; grep -q 'result="Passed"' /tmp/dsm-p01-t3.xml && ! grep -q 'result="Failed"' /tmp/dsm-p01-t3.xml</automated>
    <automated>grep -q "_dataLock" Runtime/DSMSlot.cs && grep -q "_slotsLock" Runtime/DSMSlotManager.cs && echo "locks present"</automated>
  </verify>
  <acceptance_criteria>
    - `Runtime/DSMSlot.cs` has `_dataLock` guarding all `_data` mutations in Set/Get/Has/Delete/Clear/SeedDefaults; `_watcher.Notify`/`ScheduleSave` remain outside the lock.
    - `Runtime/DSMSlotManager.cs` has `_slotsLock` guarding `_slots` mutations in GetOrCreateSlot/DeleteSlot; file deletes stay outside the lock.
    - All `DSMSlotConcurrencyTests` pass with zero Failed results.
    - No `SemaphoreSlim` introduced in this plan.
  </acceptance_criteria>
  <done>Concurrency regression test is deterministically green; shared slot state is lock-guarded per D-04/D-05 sync half.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main thread ↔ background save task | `Set`/`Get` (any thread) and `SaveAsync` debounce (task pool) touch the same `_data`/`_slots` collections |
| caller → DSMSlotManager | slot names arrive from game code / config (validated in Plan 03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | `DSMSlot._data` / `DSMSlotManager._slots` concurrent mutation | high | mitigate | Plain `lock (_dataLock)` / `lock (_slotsLock)` around all dictionary access (this plan, Task 3); regression test asserts final-state correctness |
| T-01-02 | Denial of Service | Concurrent `Dictionary` writes throwing `InvalidOperationException` mid-`Set` | medium | mitigate | Same lock guards eliminate the unsynchronized-collection exception path |
| T-01-SC | Tampering | Package/dependency supply chain | low | accept | No npm/pip/cargo installs and no new Unity package added this plan; UniTask commit-pin is deferred to Phase 5 (PERF-04). No legitimacy checkpoint required. |
</threat_model>

<verification>
- Both test asmdefs valid EditMode-only JSON referencing DMS.Runtime by the resolved GUID.
- `DSMTestRunnerSmokeTests`, `DSMSlotConcurrencyTests` all green in EditMode with zero Failed results.
- `_dataLock` present in DSMSlot; `_slotsLock` present in DSMSlotManager; `_watcher.Notify`/`ScheduleSave` and file I/O remain outside their locks.
</verification>

<success_criteria>
Test harness runs; concurrent `Set()` no longer corrupts or drops in-memory data (verified by final-state assertion); real Save→Load roundtrip persists; shared slot state is synchronized per D-04/D-05 sync half.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-thread-safety-robustness-test-infrastructure/01-01-SUMMARY.md` when done.
</output>
